# Examples

- `basic/` — minimal TODO/FIXME/HACK usage with global handlers.
- `with-custom-meta/` — Declaration Merging adds `issue`, `assignee`, `severity` to `TodoMeta`.

These are not built or tested in CI. They exist as runnable references.
